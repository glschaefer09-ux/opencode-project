#!/usr/bin/env node
/**
 * Thin shell/CI wrapper over OpenMultiAgent — no interactive session, approvals,
 * or persistence. The filesystem-tool sandbox root is configurable through the
 * orchestrator/agent JSON (`defaultCwd` / `cwd`).
 *
 * Exit codes:
 *   0 — finished; team run succeeded
 *   1 — finished; team run reported failure (agents/tasks)
 *   2 — invalid usage, I/O, or JSON validation
 *   3 — unexpected runtime error (including LLM errors)
 */
import type { SupportedProvider } from '../llm/adapter.js';
import type { AgentRunResult, TeamRunResult } from '../types.js';
export declare const EXIT: {
    readonly SUCCESS: 0;
    readonly RUN_FAILED: 1;
    readonly USAGE: 2;
    readonly INTERNAL: 3;
};
export declare const PROVIDER_REFERENCE: ReadonlyArray<{
    id: SupportedProvider;
    apiKeyEnv: readonly string[];
    baseUrlSupported: boolean;
    notes?: string;
}>;
export declare function parseArgs(argv: string[]): {
    _: string[];
    flags: Set<string>;
    kv: Map<string, string>;
};
export interface CliJsonOptions {
    readonly pretty: boolean;
    readonly includeMessages: boolean;
}
export declare function serializeAgentResult(r: AgentRunResult, includeMessages: boolean): Record<string, unknown>;
export declare function serializeTeamRunResult(result: TeamRunResult, opts: CliJsonOptions): Record<string, unknown>;
//# sourceMappingURL=oma.d.ts.map